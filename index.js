/* Inicializando variable */
const precio = 400000;

/* Selector del precio inicial del artículo */
var precioSpan = document.querySelector(".precio-inicial");
precioSpan.innerHTML = precio;

/* Selector del valor total del DOM */
var cantidadSpan = document.querySelector("span.cantidad");

/* Selector de la cantidad total*/
var valorTotalSpan = document.querySelector(".valor-total");
